{
  "name": "my-portfolio",
  "private": true,
  "description": "Next.js app is in /web. On Vercel: set Project Root Directory to web (Settings → General). Do not add a root vercel.json with --prefix web — that breaks when Root Directory is already web.",
  "scripts": {
    "dev": "npm run dev --prefix web",
    "build": "npm run build --prefix web",
    "start": "npm run start --prefix web",
    "lint": "npm run lint --prefix web"
  }
}
