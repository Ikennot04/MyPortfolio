{
  "name": "my-portfolio",
  "private": true,
  "description": "Next.js app is in /web. On Vercel: leave Root Directory empty (repo root) so vercel.json runs. If Root Directory is already set to web, delete the repo root vercel.json file.",
  "scripts": {
    "dev": "npm run dev --prefix web",
    "build": "npm run build --prefix web",
    "start": "npm run start --prefix web",
    "lint": "npm run lint --prefix web"
  }
}
