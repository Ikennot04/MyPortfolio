{
  "name": "my-portfolio",
  "private": true,
  "description": "Next.js app is in ./web. Vercel: either (A) leave Root Directory empty and use root vercel.json, or (B) set Root Directory to web and delete root vercel.json to avoid cd web/web.",
  "scripts": {
    "dev": "cd web && npm run dev",
    "build": "cd web && npm run build",
    "start": "cd web && npm run start",
    "lint": "cd web && npm run lint"
  }
}
